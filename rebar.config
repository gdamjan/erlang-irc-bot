%%-*- mode: erlang -*-

{deps, []}.
{erl_opts, [tuple_calls]}.

{profiles, [
    {prod, [
        {relx, [{dev_mode, false}]}
    ]}
]}.

{relx, [
    {release, {ircbot, "1.0.0"},
         [ircbot]},

    {dev_mode, true},
    {include_erts, false},
    {include_src, false},
    {extended_start_script, true}
]}.
