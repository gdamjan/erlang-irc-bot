{application, ircbot, [
    {description, "ircbot"},
    {vsn, git},
    {modules, [
        ircbot_app, ircbot_sup, ircbot_statem, ircbot_api,
        ircbot_lib, ircbot_plugins,
        pong_plugin, ctcp_plugin
    ]},
    {mod, {ircbot_app, []}},
    {applications, [kernel, stdlib, sasl, ssl, crypto]}]
}.
