Use: 
$ cd src/
$ erlc *.erl
$ erl
l(main). 
Pid = main:start(["irc.freenode.net", 6667, "erlbot_test", ["#lugola", "#razmjenavjestina"]]). 
Pid ! {send_data, "PRIVMSG #lugola :success"}.


l(ctcp_plugin).
gen_event:add_handler(plugins, ctcp_plugin, []).
